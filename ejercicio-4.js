$(document).ready(
    function() {

        let email = $("input");
        let emailXTipo = $("input[type=email]");
        let allInputs = $( ":input" );

        console.log(email);
        console.log(emailXTipo);
        console.log(allInput);
    }
);